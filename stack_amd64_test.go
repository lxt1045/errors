package errors

import (
	"testing"
)

func Test_gGoidOffset(t *testing.T) {
	t.Run("NewCodef", func(t *testing.T) {
		t.Logf("gGoidOffset:%d", gGoidOffset)
		t.Logf("gGoidOffset2:%d", gGoidOffset2)
	})
}
